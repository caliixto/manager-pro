import { ColorResistenciaPipe } from './color-resistencia-pipe';

describe('ColorResistenciaPipe', () => {
  it('create an instance', () => {
    const pipe = new ColorResistenciaPipe();
    expect(pipe).toBeTruthy();
  });
});
